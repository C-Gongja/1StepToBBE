type Query {
  test: Test

  # 단일 사용자 조회
  user(id: ID!): User

  # 사용자 목록 조회 (페이지네이션)
  # users(limit: Int = 10, offset: Int = 0): [User!]!

  # 사용자와 관련 데이터 함께 조회
  # userWithTodos(user_id: ID!): UserWithTodos

  # 할일 목록 조회
  # todos(user_id: ID!, status: TodoStatus): [Todo!]!

  # 일정 조회
  # schedules(user_id: ID!, date: String): [Schedule!]!
}
